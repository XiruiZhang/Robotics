package ca.mcgill.ecse211.wallfollowing;

public class dataProcessor {
	public static int maxDist=255;
	// this method filters out the noise of the data
	// the logic is if the distance measured by sensor exceeds certain points
	// then it is considered noise ( scrambled signal ) or inference ( gap in block)
	public void filterData(float data) {
		if (data>=215);
			
	}
}
